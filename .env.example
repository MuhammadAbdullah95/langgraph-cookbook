# Copy this file to `.env` and fill in your keys.
# `.env` is gitignored — never commit real keys.

# ---- Required for ALL notebooks ----
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# ---- Required for notebook 06 (research-agent example) and agentic_rag.ipynb ----
# Free 1000 searches/month at https://tavily.com/
TAVILY_API_KEY=

# ---- Required for agentic_rag.ipynb only ----
# Run a local Qdrant in Docker:  docker run -p 6333:6333 qdrant/qdrant
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
